# Next.js Web App Environment Variables
# Based on npm create sanity@latest -- --template robotostudio/turbo-start-sanity --project jyrid0xg --dataset production

# Your Sanity Project ID from the command
NEXT_PUBLIC_SANITY_PROJECT_ID=jyrid0xg

# Your Sanity Dataset from the command
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API version - typically a date
NEXT_PUBLIC_SANITY_API_VERSION=2024-03-06

# URL to your Sanity Studio
# In development: use localhost on studio port
NEXT_PUBLIC_SANITY_STUDIO_URL=http://localhost:3333
# In production: use your deployed studio URL
# NEXT_PUBLIC_SANITY_STUDIO_URL=https://your-studio-url.netlify.app

# Sanity API tokens - you'll need to generate these in your Sanity dashboard
# Token with read permissions for public content
SANITY_API_READ_TOKEN=your-read-token-here

# Token with write permissions (only needed if your site writes to Sanity)
# Can be left empty if your site only reads from Sanity
SANITY_API_WRITE_TOKEN=

# Disable Sanity CDN caching for development to ensure fresh data
NEXT_PUBLIC_SANITY_USE_CDN=false
